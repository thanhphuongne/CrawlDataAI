import { NewPasswordView } from 'src/sections/auth/jwt';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Auth Classic: Forgot Password',
};

export default function NewPasswordPage() {
  return <NewPasswordView />;
}
