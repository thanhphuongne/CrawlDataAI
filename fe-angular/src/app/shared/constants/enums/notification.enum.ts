export enum NOTIFICATION_EVENT_TYPE {
    CREATE = "Tạo mới Agent",
    UPDATE = "Cập nhật Agent"
}

export enum NOTIFICATION_AGNET_STATUS {
    BUILDING = "Đang build",
    SUCCESS = "Build thành công",
    FAILED = "Build lỗi",
}
